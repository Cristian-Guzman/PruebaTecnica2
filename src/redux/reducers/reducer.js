import { types } from "../types/types";

const reducer = (state= [], action) => {
    switch (action.Types) {
        case types.mayorPrecio:
            return
        case types.menorPrecio:
            return
        default:
            break;
    }
}

export default reducer